# Foto del chofer

Guardá la foto en esta carpeta, preferentemente como WebP vertical y con menos de 500 KB.

Después abrí `src/data/projectConfig.ts` y configurá:

```ts
driverImage: "images/driver/mi-chofer.webp",
```

`driver-car.webp` es el auto transparente utilizado en la animación previa.
