import type { Memory } from "../../types/memory";

/** Repositorio local de muestra. En V0.5 podrá cambiarse por una API sin tocar la UI. */
export async function getMemories(): Promise<Memory[]> {
  return [];
}
